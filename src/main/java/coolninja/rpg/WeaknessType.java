package coolninja.rpg;

/**
 * List of weakness types
 * @version 1.0
 * @since 1.0
 * @author Ben Ballard
 */
public enum WeaknessType {
    
    Fire(), Water(), Earth(), Air(), Physical(), Magic(), Dark(), Light(), Sound();
}
